'use strict';


module.exports = function(app) {
  var connections = require('../../app/controllers/connections.server.controller');

  // Connections Routes
  //app.route('/connection/:url').get(connections.getConnectionInfo);
  //app.route('/connection-by-code/:code').get(connections.getUrlByCode);
};
